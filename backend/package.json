{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm rebuild better-sqlite3 && pip install -r requirements.txt || echo 'Python dependencies installation failed, ML features may not work'"
  },
  "dependencies": {
    "@google/genai": "^0.3.0",
    "better-sqlite3": "^12.4.5",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "fs-extra": "^11.2.0",
    "groq-sdk": "^0.36.0",
    "multer": "^2.0.2",
    "xlsx": "^0.18.5"
  }
}
